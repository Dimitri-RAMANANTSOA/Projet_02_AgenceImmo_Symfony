{% extends 'base.html.twig' %}

{% block title %}Agence VLM{% endblock %}

{% block body %}
<div class="p-5 mb-4 bg-light rounded-3">
    <div class="container">
    <h1>Agence VLM</h1>
    <p>
    Lorem ipsum dolor sit amet, consectetur adipisicing elit. Cum societes natoque penatibus et magnis disputanci. Lorem ipsum dolor sit am
    </p>
    </div>
</div>

<div class="container">
    <h2>Les derniers biens</h2>
    <div class="row flex">
        {% for property in properties %}
            <div class="col-3">
                <div class="card">
                    <div class="card-body">
                        <h5 class="card-title">
                            <a href="{{ path('property.show', {id: property.id, slug: property.slug}) }}">{{ property.title }}</a>
                        </h5>
                        <p class="card-text">{{ property.city }} ({{ property.PostalCode}})</p>
                        <div class="text-primary" style="font-weight: bold; font-size: 2rem;">{{ property.FormattedPrice }} €</div>
                    </div>
                </div>
            </div>
        {% endfor %}
    </div>
</div>
{% endblock %}
